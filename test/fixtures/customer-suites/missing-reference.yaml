schema_version: aw-suite/1
suite_id: customer.missing.reference
title: Missing reference
synthetic_only: true
cases:
  - case_id: missing.ref
    turns:
      - content: What is the policy?
    expected:
      facts:
        - A fact
    criteria:
      - criterion_id: missing.ref.required
        requirement: required
        kind: llm_rubric
        statement: Uses a missing reference.
        reference_ids: [does-not-exist]
    reference_ids: [does-not-exist]
