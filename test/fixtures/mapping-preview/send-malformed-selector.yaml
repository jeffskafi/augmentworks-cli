version: 1

target:
  name: chat-preview-malformed
  connector: http
  base_url: http://127.0.0.1:9
  operations:
    send:
      method: POST
      path: /chat
      response:
        content: "$.answer."

telemetry:
  allow_tool_events: false
